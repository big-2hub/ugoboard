# ウゴボード（UgoBoard）

## プロジェクト概要

ミニバス向けの「動く作戦盤」PWA。コーチが作戦や練習ドリルの動きをアニメーションで子どもに見せるアプリ。

## 技術スタック

- TypeScript / React / Vite
- Konva.js（描画）
- Dexie.js（IndexedDB）
- vite-plugin-pwa
- Vercel

## 設計原則

- 座標はすべてコート正規化座標（0〜1）で保持する。ピクセル変換は描画層だけで行う。
- コート定義は `CourtConfig` データとして分離し、ハードコードを禁止する。将来の一般コート・他競技・3Pライン追加に対応できる形にする。
- Dexie のスキーマバージョン管理を最初から使う。
- タッチ前提UI（指サイズのボタン）、オフラインファーストで設計する。

## 作業ルール

- 1依頼につき、1つの動く成果物を完成させる。
- Codexの担当範囲は、実装・テスト・Notion実装ログの作成までとする。
- CodexはGitHubへのコミットとpushを行わない。
- ユーザーがGitHub Desktopで差分を確認し、コミットして`main`へpushする。
- 実装後は Notion の「バスケ作戦盤アプリ開発」配下に「Codex実装ログ:ステップ◯」ページを作る。
- 実装ログには「やったこと」「主要ファイル」「設計判断」「未対応・懸念」を記載する。
